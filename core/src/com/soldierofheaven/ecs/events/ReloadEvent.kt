package com.soldierofheaven.ecs.events

import net.mostlyoriginal.api.event.common.Event

// todo: there should be 2 events: one that tries reloading the weapon in WeaponSystem, and the other that is sent to update UI (show reload bar)
class ReloadEvent : Event
